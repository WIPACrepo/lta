version: 2
jobs:
    test:
        docker:
            - image: circleci/python:3.7.2
        steps:
            - checkout
            - run: ./setupenv.sh
            - run: |
                . env/bin/activate &&
                ./snake rebuild
workflows:
    version: 2
    build_and_test:
        jobs:
            - test